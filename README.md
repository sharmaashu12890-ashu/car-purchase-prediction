# 🚗 Car Purchase Prediction Using Logistic Regression

## 📌 Project Overview

This project uses **Machine Learning** to predict whether a customer is likely to purchase a car based on their **Age** and **Estimated Salary**.

A **Logistic Regression** classification algorithm is trained on customer data to predict the target variable `Purchased`.

The project also includes basic data exploration, data preprocessing, visualization, model training, and prediction comparison.

---

## 🎯 Objective

The main objective of this project is to build a machine learning model that can predict:

- `0` → Customer will **not purchase** the car
- `1` → Customer will **purchase** the car

The prediction is based on:

- 👤 Age
- 💰 Estimated Salary

---


🛠️ Technologies Used
Python
NumPy
Pandas
Matplotlib
Scikit-learn
Jupyter Notebook



## 📊 Dataset

The project uses a CSV dataset named:

```text
suv_data.csv